/* isprime.c: determines if an entered number is prime.*/
int main(char **argv, int argc)
{
    int input_num;
    fscanf("enter a number", %d, inputing);
    while (!isprime(input_num))
        isprime(input_num);
}
isprime(input_num) {
    if isint(input_num/2)
